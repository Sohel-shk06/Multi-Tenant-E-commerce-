/**
 * ThemeProvider — placeholder for theme management.
 * Currently the app uses a light theme only via Tailwind CSS.
 * TODO: Add dark mode toggle using Tailwind's dark variant if required.
 */
function ThemeProvider({ children }) {
  return children
}

export default ThemeProvider
