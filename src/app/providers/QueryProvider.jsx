/**
 * QueryProvider — placeholder for React Query (TanStack Query) setup.
 * TODO: Install @tanstack/react-query and replace this with QueryClientProvider.
 * TODO: Configure defaultOptions for staleTime, retry, refetchOnWindowFocus.
 */
function QueryProvider({ children }) {
  // TODO: Wrap with <QueryClientProvider client={queryClient}>
  return children
}

export default QueryProvider
